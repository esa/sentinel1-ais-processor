[project]
name = "sentinel-1 ais processor"
version = "0.1"
description = "End to end processor for Sentinel-1 C&D AIS instrument source packets"
requires-python = ">=3.10.11"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]